services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    volumes:
      - ./backend/backups:/app/backups

  mongo:
    volumes:
      - /var/lib/budgeter/mongo:/data/db
